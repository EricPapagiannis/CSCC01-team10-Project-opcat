Team 10 OEC Updater Manual

An utility for updating the open exoplanet catelogue with external sources

Copyright (c) 2016 [Chain Coders], All Right Reserved

----------------------------------------------------------------

Valid Commands

help

	no args

	prints information about program usage

update

	no args

	updates system from set up remote databases; generates and
	presents proposed changes to user

showall

	no args

	presents all proposed changes to user

acceptall

	no args

	approves all proposed changes with many branches with one
	pull request on each branch

acceptall2

	no args

	approves all proposed changes with one pull request on one
	branch, with many commits

denyall

	no args

	denies all proposed all proposed changes and sends them
	to the blacklist

status

	no args

	view information about the current system

postponeall

	no args

	denies all proposed changes, without sending them to the
	blacklist

clearblacklist

	no args

	removes all proposed changes currently in blacklist

stopautoupdate

	no args

	stops the autoupdate daemon

clearrepo

	no args

	clears the configured repo back to the default

shownumber

	arg: [int]

	displays the proposed change filed under specified number

accept

	arg: [int] or [int]-[int] or "s"-"e"

	accepts a single proposed change filed under that number,
	or range of number seperated by a "-"

	uses many branches with one pull request on each branch

accept2

	arg: [int] or [int]-[int] or "s"-"e"

	accepts a single proposed change filed under that number,
	or range of number seperated by a "-"

	uses one single branch, with many commits on each

deny

	arg: [int] or [int]-[int] or "s"-"e"

	denies a single proposed change filed under that number,
	or range of number seperated by a "-", and sends them
	to the blacklist

showrange

	arg: [int]-[int] or "s"-"e"

	displays a range of proposed changes filed by the
	specified input

postpone

	arg: [int] or [int]-[int] or "s"-"e"

	denies a single proposed change filed under that number,
	or range of number seperated by a "-", without sending it
	to the blacklist

setautoupdate

	arg: [int]

	starts the autoupdate daemon which will run update
	every provided hours

showlastest

	arg: [int]

	show lastest provided number of prposed changes

setrepo

	arg: [str]

	sets the repo for the pull requests to be sent to